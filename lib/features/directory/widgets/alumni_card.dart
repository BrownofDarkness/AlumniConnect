import 'package:flutter/material.dart';

class AlumniCard extends StatelessWidget {
  const AlumniCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}