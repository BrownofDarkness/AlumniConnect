import 'package:flutter/material.dart';

class AddAlumniScreen extends StatelessWidget {
  const AddAlumniScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Add Alumni Screen'),
      ),
    );
  }
}