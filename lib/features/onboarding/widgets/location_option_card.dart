import 'package:flutter/material.dart';

class LocationOptionCard extends StatelessWidget {
  const LocationOptionCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}