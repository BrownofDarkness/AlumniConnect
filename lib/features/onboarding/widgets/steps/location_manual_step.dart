import 'package:flutter/material.dart';

class LocationManualStep extends StatelessWidget {
  const LocationManualStep({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Location Manual Step'),
    );
  }
}