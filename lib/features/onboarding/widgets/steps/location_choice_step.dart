import 'package:flutter/material.dart';

class LocationChoiceStep extends StatelessWidget {
  const LocationChoiceStep({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Location Choice Step'),
    );
  }
}