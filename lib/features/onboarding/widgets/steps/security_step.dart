import 'package:flutter/material.dart';

class SecurityStep extends StatelessWidget {
  const SecurityStep({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Security Step'),
    );
  }
}