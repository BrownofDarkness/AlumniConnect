import 'package:flutter/material.dart';

class ContactStep extends StatelessWidget {
  const ContactStep({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Contact Step'),
    );
  }
}