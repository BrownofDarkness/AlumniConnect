import 'package:flutter/material.dart';

class IdentityStep extends StatelessWidget {
  const IdentityStep({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Identity Step'),
    );
  }
}