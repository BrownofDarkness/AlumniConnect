import 'package:flutter/material.dart';

/// Design tokens couleur d'AlumniConnect.
///
/// Reprend la palette validée dans la charte graphique.
class AppColors {
  AppColors._();

  // Marque
  static const Color navy = Color(0xFF0A2540);
  static const Color primary = Color(0xFF2B4A8B);
  static const Color primaryLight = Color(0xFF3F65B0);
  static const Color amber = Color(0xFFF5A623);
  static const Color amberSoft = Color(0xFFFEF3E7);

  // Neutres
  static const Color cream = Color(0xFFF5F7FA);
  static const Color ink = Color(0xFF1A1F2B);
  static const Color muted = Color(0xFF5A6472);
  static const Color divider = Color(0xFFDDE3EC);
  static const Color white = Color(0xFFFFFFFF);

  // Sémantique
  static const Color success = Color(0xFF059669);
  static const Color warning = Color(0xFFD97706);
  static const Color error = Color(0xFFDC2626);
  static const Color info = Color(0xFF2B4A8B);
}
